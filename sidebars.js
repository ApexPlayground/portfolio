/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  projectsSidebar: [
    // List each project doc directly; no wrapping "Projects" category
    'intro',
    's3Scan', // add more project markdown files here
  ],
};

export default sidebars;
